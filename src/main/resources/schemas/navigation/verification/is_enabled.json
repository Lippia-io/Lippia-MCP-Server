{
  "type": "object",
  "title": "Verifica una única vez a que un elemento esté habilitado",
  "properties": {
    "by": {
      "type": "string",
      "enum": ["css", "xpath", "id", "name", "class", "tag", "accessibility_id", "android_ui_automator", "ios_ns_predicate", "custom", "image"],
      "description": "El tipo de selector a utilizar"
    },
    "selector": {
      "type": "string",
      "description": "El selector del elemento"
    }
  },
  "required": ["by", "selector"]
}