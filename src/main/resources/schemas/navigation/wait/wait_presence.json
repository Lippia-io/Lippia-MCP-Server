{
  "type": "object",
  "title": "Waits for an element to be present",
  "properties": {
    "by": {
      "type": "string",
      "enum": ["css", "xpath", "id", "name", "class", "tag", "accessibility_id", "android_ui_automator", "ios_ns_predicate", "custom", "image"],
      "description": "The type of selector to use"
    },
    "selector": {
      "type": "string",
      "description": "The element selector"
    }
  },
  "required": ["by", "selector"]
}