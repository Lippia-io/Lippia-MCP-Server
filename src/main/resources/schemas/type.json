{
  "type": "object",
  "title": "Types text into an input field",
  "properties": {
    "by": {
      "type": "string",
      "enum": ["css", "xpath", "id", "name", "class", "tag", "accessibility_id", "android_ui_automator", "ios_ns_predicate", "custom", "image"],
      "description": "The type of selector to use"
    },
    "selector": {
      "type": "string",
      "description": "The CSS or XPath selector of the input field"
    },
    "text": {
      "type": "string",
      "description": "The text to type"
    }
  },
  "required": ["by", "selector", "text"]
} 