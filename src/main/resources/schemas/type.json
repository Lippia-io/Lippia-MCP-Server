{
  "type": "object",
  "title": "Escribe texto en un campo de entrada",
  "properties": {
    "by": {
      "type": "string",
      "enum": ["css", "xpath", "id", "name", "class", "tag", "accessibility_id", "android_ui_automator", "ios_ns_predicate", "custom", "image"],
      "description": "El tipo de selector a utilizar"
    },
    "selector": {
      "type": "string",
      "description": "El selector CSS o XPath del campo de entrada"
    },
    "text": {
      "type": "string",
      "description": "El texto a escribir"
    }
  },
  "required": ["by", "selector", "text"]
} 